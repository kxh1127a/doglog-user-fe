import React from 'react';
import styles from '@/styles/Nav.module.scss'

const Nav = () => {
    return (
        <div className={styles.nav}>
            navBar
        </div>
    );
};

export default Nav;